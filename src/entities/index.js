const QuoteRequest = require('./quote-request');
const Parcel = require('./parcel');
const ShippingAddress = require('./shipping-address');
const Shipment = require('./shipment');
const ShipmentStatus = require('./shipment-status');

module.exports = {
  QuoteRequest,
  Parcel,
  ShippingAddress,
  Shipment,
  ShipmentStatus,
};
